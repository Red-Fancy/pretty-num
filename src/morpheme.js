/**
 * @param {string} str
 * @param {string} suffix
 * @return {string}
 */
export const addSuffix = (str, suffix) => `${str}${suffix}`;

/**
 * @param {string} str
 * @param {string} prefix
 * @return {string}
 */
export const addPrefix = (str, prefix) => `${prefix}${str}`;
